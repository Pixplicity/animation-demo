<?xml version="1.0" encoding="utf-8"?>
<rotate xmlns:android="http://schemas.android.com/apk/res/android"
        android:duration="4000"
        android:fromDegrees="0"
        android:pivotX="50%"
        android:pivotY="50%"
        android:toDegrees="3600">

    <shape
        android:innerRadius="20dp"
        android:shape="ring"
        android:thickness="1dp"
        android:useLevel="false">
        <size
            android:height="48dp"
            android:width="48dp"/>

        <gradient
            android:centerColor="#80ec7e2a"
            android:centerY="0.0"
            android:endColor="#ffec7e2a"
            android:startColor="#00ec7e2a"
            android:type="sweep"
            android:useLevel="false"/>
    </shape>

</rotate>

    <!--
        Animation speed

        The rotation speed of the indeterminate progress bar is fixed at one revolution every 4 seconds, changing the duration attribute in the drawable XML has no effect.
        If you prefer to speed it up, you can change the toDegrees attribute to multiples of 360:

        720 makes one turn in 2 seconds
        1080 makes one turn in 1.33 seconds
        1440 makes one turn in 1 second
    -->